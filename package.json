{
  "name": "@dragonlabs/redux-cache-key-util",
  "version": "0.0.3",
  "main": "index.js",
  "files": [
    "build/**/*"
  ],
  "license": "Apache-2.0",
  "repository": "https://github.com/markormesher/dragonlabs-redux-cache-key-util",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "build-test": "tsc -p ./tsconfig.test.json",
    "clean": "rimraf ./build ./coverage ./.nyc_output",
    "lint": "eslint --ext ts ./src",
    "run-test": "nyc mocha \"./build/**/*.js\"",
    "test": "run-s clean build-test run-test"
  },
  "dependencies": {
    "redux": ">= 4.0.0"
  },
  "devDependencies": {
    "@dragonlabs/eslint-config": "^0.0.4",
    "@types/chai": "^4.2.10",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.9.0",
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "lockfile-lint": "^4.0.0",
    "mocha": "^7.1.0",
    "npm-run-all": "^4.1.5",
    "nyc": "^15.0.0",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  }
}
